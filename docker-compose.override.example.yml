# docker-compose.override.yml 示例
# 复制此文件为 docker-compose.override.yml 并按需修改
# docker compose 会自动加载 override 文件

services:
  # --- 端口偏移示例（避免与宿主机已有服务冲突）---
  # api-gateway:
  #   ports:
  #     - "18080:8080"   # 宿主机 18080 -> 容器 8080

  # frontend:
  #   ports:
  #     - "13000:80"     # 宿主机 13000 -> 容器 80

  # postgres:
  #   ports:
  #     - "15432:5432"

  # redis:
  #   ports:
  #     - "16379:6379"

  # --- 启用 Redis 密码（取消注释并设置密码）---
  # redis:
  #   command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "--no-auth-warning", "-a", "${REDIS_PASSWORD}", "ping"]

  # data-service:
  #   environment:
  #     - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0

  # user-service:
  #   environment:
  #     - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/1

  # --- 需要宿主机直连 payment-service 时取消注释 ---
  # payment-service:
  #   ports:
  #     - "8085:8085"
