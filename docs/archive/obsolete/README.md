# Obsolete Docs

本目录存放已被后续版本替代的历史文档，保留仅用于追溯，不作为当前执行依据。

## 迁入规则

- 同主题存在 `final`、`修复版`、更新日期更晚版本时，旧迭代文档迁入本目录。
- 命名含 `updated`、`follow_up`、`round`、`progress_tracking` 的阶段性材料，迭代完成后迁入本目录。
- 不确定是否重复的文档暂不迁入，避免误归档。

## 使用建议

- 日常查阅优先 `docs/` 顶层和 `docs/archive/audit-reports/`。
- 仅在需要追溯演进过程时查阅本目录。
